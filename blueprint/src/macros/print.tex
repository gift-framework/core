% GIFT Blueprint - Print-only Macros
% Dummy macros for PDF (leanblueprint provides these for web)

% Dummy macros that make sense only for web version
\newcommand{\lean}[1]{}
\newcommand{\leanok}{}
\newcommand{\mathlibok}{}
\newcommand{\notready}{}
\newcommand{\discussion}[1]{}

% Make sure that arguments of \uses and \proves are real labels
% by using invisible refs (LaTeX warns if label undefined)
\ExplSyntaxOn
\NewDocumentCommand{\uses}{m}
 {\clist_map_inline:nn{#1}{\vphantom{\ref{##1}}}%
  \ignorespaces}
\NewDocumentCommand{\proves}{m}
 {\clist_map_inline:nn{#1}{\vphantom{\ref{##1}}}%
  \ignorespaces}
\ExplSyntaxOff
